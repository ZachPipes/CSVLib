#include "library.h"

#include <string>
#include <vector>

// Create headers as strings that are equal to 0, 1, 2, 3, etc

template <FILE* input, typename... Args>
std::vector<std::string> parse(FILE* inputFile, Args&&... args) {
    std::vector<std::string> parsedFile;



    return parsedFile;
}

void print(std::vector<std::string> inputFile) {

}